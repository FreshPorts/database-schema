--
-- $Id: datatype.txt,v 1.22 2006-11-04 19:41:31 dan Exp $
--
-- Copyright (c) 1998-2006 DVL Software Limited
--

  DROP TYPE commit_record CASCADE;
CREATE TYPE commit_record AS (
	commit_log_id        integer,
	commit_date_raw      timestamp with time zone,
	message_subject      text,
	message_id           text,
	committer            text,
	commit_description   text,
	commit_date          text,
	commit_time          text,
	encoding_losses      boolean,
	port_id              integer,
	needs_refresh        smallint,
	forbidden            text,
	broken               text,
	deprecated           text,
	ignore               text,
	element_id           integer,
	version              text,
	revision             text,
	epoch                text,
	date_added           integer,
	short_description    text,
	category_id          integer,
	port                 text,
	status               character(1),
	category             text,
	vulnerable_current   integer,
	vulnerable_past      integer,
    restricted           text,
    no_cdrom             text,
    expiration_date      date,
    is_interactive       text,
    only_for_archs       text,
    not_for_archs        text,
	watch                bigint,
	pathname             text
);

  DROP TYPE element_type CASCADE;
CREATE TYPE element_type AS (
	id         integer,
	name       text,
	type       text,
	status     text,
	iscategory boolean,
	isport     boolean
);

  DROP TYPE freshports_commit_record CASCADE;
CREATE TYPE freshports_commit_record AS (
	port_id            integer,
	short_description  text,
	category           text,
	message_id         text,
	committer          text,
	commit_description text,
	commit_log_id      integer,
	port               text,
	commit_date        text,
	commit_time        text,
	encoding_losses    boolean,
	status             character(1),
	port_version       text,
	port_revision      text,
	port_epoch         text,
	revision_name      text,
	change_type        text,
	pathname           text,
    is_port            boolean,
	element_id         integer,
	forbidden          text,
	deprecated         text,
	ignore             text,
	broken             text,
	vulnerable_current integer,
	vulnerable_past    integer,
    restricted         text,
    expiration_date    date,
    no_cdrom           text,
    is_interactive     text,
    only_for_archs     text,
    not_for_archs      text,
	watch              bigint
);

  DROP TYPE logincounts_record CASCADE;
CREATE TYPE logincounts_record AS (
	date            date,
	count           integer
);

  DROP TYPE vuxml_record CASCADE;
CREATE TYPE vuxml_record AS (
	id				integer,
	vid				text,
	package_name	text,
	op1				text,
	v1				text,
	op2				text,
	v2				text
);

  DROP TYPE also_watched_record CASCADE;
CREATE TYPE also_watched_record AS (
	element_id			integer,
	url					text
);

  DROP TYPE old_new_ports_record CASCADE;
CREATE TYPE old_new_ports_record AS (
	name_old			text,
	category_old		text,
	name_new			text,
	category_new		text
);
